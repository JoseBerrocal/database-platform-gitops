apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elastic
  namespace: elasticsearch
spec:
  version: 9.1.0
  nodeSets:
  - name: default
    count: 2
    config:
      node.store.allow_mmap: false
